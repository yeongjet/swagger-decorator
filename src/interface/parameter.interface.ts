export enum DecoratorParameterLocation {
    QUERY = 'query',
    PATH = 'path',
    HEADERS = 'header'
}

export enum BindingParameterLocation {
    BODY = 'body',
    QUERY = 'query',
    PATH = 'path',
    HEADERS = 'header'
}
